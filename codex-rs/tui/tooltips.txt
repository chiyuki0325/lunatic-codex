对话较长时，使用 /compact 总结历史并释放上下文。
使用 /new 开始新的想法；此前会话会保留在历史记录中。
如发现异常，使用 /feedback 向维护者发送日志。
使用 /model 快速切换模型或推理强度。
使用 /permissions 控制 Codex 何时请求确认。
运行 /review 获取当前更改的代码审查。
使用 /skills 列出可用技能，或让 Codex 使用某个技能。
使用 /status 查看当前模型、批准状态和令牌用量。
使用 /statusline 配置状态栏中显示的项目。
使用 /fork 将当前对话分支为新线程。
使用 /side 在临时分支中开始侧边对话，避免影响主线程。
使用 /init 创建包含项目专属指引的 AGENTS.md。
使用 /mcp 列出已配置的 MCP 工具。
使用 /personality 自定义 Codex 的沟通方式。
使用 /rename 重命名线程，以便更轻松地恢复线程。
如有 API 问题，可使用 OpenAI 文档 MCP；通过 `codex mcp add openaiDeveloperDocs --url https://developers.openai.com/mcp` 启用。
加入 OpenAI 社区 Discord：http://discord.gg/openai
访问 Codex 社区论坛：https://community.openai.com/c/codex/37
可在 Codex 中使用 `!` 运行任何 shell 命令（例如 `!ls`）。
输入 / 打开命令弹窗；Tab 可自动补全斜杠命令。
输入框为空时，按下 Esc 可返回并编辑上一条消息；按下 Enter 确认。
任务运行时按下 Tab 可将消息加入队列；否则将立即发送（`!` 除外）。
~/.codex/config.toml 中的 [tui.keymap] 可让您重新绑定支持的快捷键。
请参阅 Codex 键位映射文档，了解支持的操作和示例。
按下 Ctrl+V 粘贴图像，以附加到下一条消息。
运行 `codex resume` 可恢复之前的对话。
使用 /copy 或按下 Ctrl+O，将最新的智能体回复复制为 Markdown。
