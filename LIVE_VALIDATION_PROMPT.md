# Multi-agent V2 TUI 人工验证提示词

> 注意：本验证会实际启动多个 agent，并产生额外模型用量。请在测试环境中使用。
>
> 将下面代码块中的内容完整粘贴到一个启用了 Multi-agent V2 的新 Codex 会话中。验证期间不要让 Codex 修改代码；测试者按提示手动操作 TUI。

```text
这是一次 Multi-agent V2 TUI 交互验证。不要修改任何文件，也不要执行破坏性操作。请严格按以下要求建立一个适合人工观察的 agent 拓扑：

1. 在本轮尽快启动至少 6 个直属 subagent，并给它们分配含义明显、彼此不同的中文任务，例如：
   - 调研后端 agent 状态流转
   - 检查 TUI selector 渲染逻辑
   - 核对 role 与名称展示规则
   - 检查窄终端文本截断
   - 分析 agent 列表滚动行为
   - 汇总非阻塞切换验收点
2. 使用不同的可用 role；不要为了验证而修改 Multi-agent V2 工具名称、description、参数 schema 或 required 字段。
3. 明确要求“调研后端 agent 状态流转”的 subagent 再启动至少 1 个孙级 agent，给孙级 agent 一个含义明显的中文任务名称，并继续执行自己的任务。不要把所有任务都放在同一层。
4. 让其中 2 个直属 subagent 完成简短检查并进入空闲状态；让至少 3 个 agent 执行需要持续阅读和分析的较长任务，以便人工检查时仍处于运行状态。不要使用 sleep、忙等、死循环或无意义的长命令来拖延。
5. 启动完拓扑后，不要等待所有 agent 完成，不要取消、关闭、中断或暂停任何 agent。保持当前会话可交互，并立即停下来告诉我：“agent 拓扑已建立，请开始人工检查”。在我明确回复“检查完成”之前，不要汇总最终结果。
6. 如果某个长任务提前结束，可以向仍存在的 agent 追加有意义的只读检查，使人工检查期间尽量同时存在运行中与空闲 agent；仍然不得使用人为延时。

当你告诉我可以开始检查后，我会手动验证以下项目；你不需要代替我操作按键：

- 普通模式下，底栏下面出现右对齐提示；有空闲 agent 时包含空闲总数。
- selector 最多显示 4 个 agent，超过 4 个时可滚动，而不是额外占用更多行。
- 根 agent 显示语义名称；缺少名称时回退为 `Codex`，并显示 `[role]`；缺少 role 时显示 `[default]`。
- 直属与孙级 agent 有清晰的层级缩进。
- 运行中 agent 使用绿色实心圆 `●`；空闲 agent 的圆点使用终端默认正文颜色，而非显式白色。
- 状态文字和持续时间会更新，并能同时观察到“运行中”和“空闲”。
- 空输入框中按 `↓` 进入选择模式，提示变为 `↑/↓ 选择 · Enter 以聚焦该 agent · Esc 返回`。
- 选择模式中使用 `↑/↓` 移动候选项，行首 `>` 跟随候选项；滚动后候选项始终可见。
- 按 `Enter` 可立即聚焦所选 agent，即使当前 agent 或目标 agent 正在运行，也不得等待、暂停、中断或取消任务。
- 按 `Esc` 返回普通输入模式。
- 空闲或已关闭的 agent 即使不计入普通预览，仍可在选择模式中被选中；数量统计以全部 agent 为准。
- 将终端缩窄后，名称只在空间不足时以省略号截断；恢复宽度后显示完整名称，右侧状态仍保持可见。

除建立和维持上述验证场景外，不要自行操作 selector，也不要提前结束验证。
```
